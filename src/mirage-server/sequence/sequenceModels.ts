import { Model } from 'miragejs';

import { TSequence } from 'src/entities/Sequence/types';
import { ModelDefinition } from 'miragejs/-types';

export const SequenceModel: ModelDefinition<TSequence> = Model.extend({});

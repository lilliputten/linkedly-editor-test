import { Model } from 'miragejs';

import { TCampaign } from 'src/entities/Campaign/types';
import { ModelDefinition } from 'miragejs/-types';

export const CampaignModel: ModelDefinition<TCampaign> = Model.extend({});
